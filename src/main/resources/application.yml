spring:
  application:
    name: curso
  datasource:
    url: jdbc:mysql://localhost:3306/dev_db
    username: jose
    password: josepass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false

server:
  port: 8085
